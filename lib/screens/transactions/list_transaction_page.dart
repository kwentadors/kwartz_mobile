import 'package:flutter/material.dart';

class ListTransactionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Transactions")),
      body: Center(
        child: Text("List transactions pag"),
      ),
    );
  }
}
