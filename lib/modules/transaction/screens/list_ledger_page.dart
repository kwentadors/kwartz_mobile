import 'package:flutter/material.dart';

class ListLedgerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Ledger")),
      body: Center(
        child: Text("Ledger page"),
      ),
    );
  }
}
